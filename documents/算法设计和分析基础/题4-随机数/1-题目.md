# 随机数

## 线性同余法

**算法**
```
Random(n, m, seed, a, b)
// 根据线性同余法生成n个伪随机数的一个序列
// 输入: 一个正整数n和整数参数m, seed, a, b
// 输出: n个伪随机数的一个序列, 是均匀分布在0和m-1区间内的整数值
// 注意: 我们可以把生成的整数作为小数点后面的数字
r0 ← seed
for i ← 1 to n do
    ri ← (a * r(i-1) + b) mode m
```